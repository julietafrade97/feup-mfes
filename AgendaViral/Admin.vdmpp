class Admin is subclass of User
types

values

instance variables

operations
		-- Constructor
		public Admin : String * String ==> Admin
		Admin(mail, passw) == (
			UserInit(mail, passw);
			return self;
		);
		
		-- Returns if user is admin
		public isAdmin: () ==> bool
		isAdmin() == return true;

functions

traces

end Admin